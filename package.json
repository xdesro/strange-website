{
  "name": "strange.website",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently 'sass --no-source-map --watch ./scss:./css' 'npx @11ty/eleventy --serve'",
    "build": "sass --no-source-map --style compressed ./scss:./css && npx @11ty/eleventy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^1.0.1",
    "concurrently": "^7.2.1",
    "sass": "^1.52.2"
  }
}
